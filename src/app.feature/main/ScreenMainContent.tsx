import React from 'react';
import styled from 'styled-components';

const ScreenMainContent: any = () => {
  return <StyledWrapper>메인 퀴즈</StyledWrapper>;
};

export default ScreenMainContent;

const StyledWrapper = styled.div`
  width: 100%;
  display: flex;
`;
