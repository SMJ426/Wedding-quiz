import React from 'react';
import styled from 'styled-components';

const ScreenQuizMain: React.FC = ({}) => {
  return <StyledWrapper>퀴즈메인</StyledWrapper>;
};

export default ScreenQuizMain;

const StyledWrapper = styled.div``;
